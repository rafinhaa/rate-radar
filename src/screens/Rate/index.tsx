import { SafeAreaView, Text } from "react-native"

export type RateProps = {}

const Rate = (_props: RateProps) => {
  return (
    <SafeAreaView className="flex-1 bg-blue-950">
      <Text>Rate</Text>
    </SafeAreaView>
  )
}

export default Rate
